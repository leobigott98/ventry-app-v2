import { NextRequest,NextResponse } from "next/server";
import { requireApiCommunityContext } from "@/lib/auth/api";
import { removeManagedEventGuest,updateManagedEventGuest } from "@/lib/domain/access-lifecycle";
import { removeManagedMemberSchema,updateManagedEventGuestSchema } from "@/lib/schemas/access-lifecycle";
import { lifecycleErrorResponse } from "@/lib/server/lifecycle-response";
export async function PUT(request:NextRequest,{params}:{params:Promise<{eventId:string;guestId:string}>}){const auth=await requireApiCommunityContext(request,["admin","resident"]);if("response"in auth)return auth.response;const parsed=updateManagedEventGuestSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message??"Datos inválidos."},{status:400});const{eventId,guestId}=await params;try{return NextResponse.json({ok:true,...await updateManagedEventGuest(auth.context.community.id,eventId,guestId,parsed.data),redirectTo:`/app/events/${eventId}`});}catch(error){return lifecycleErrorResponse("update_event_guest",error);}}
export async function DELETE(request:NextRequest,{params}:{params:Promise<{eventId:string;guestId:string}>}){const auth=await requireApiCommunityContext(request,["admin","resident"]);if("response"in auth)return auth.response;const parsed=removeManagedMemberSchema.safeParse(await request.json());if(!parsed.success)return NextResponse.json({error:parsed.error.issues[0]?.message??"Datos inválidos."},{status:400});const{eventId,guestId}=await params;try{return NextResponse.json({ok:true,...await removeManagedEventGuest(auth.context.community.id,eventId,guestId,parsed.data.expectedVersion,parsed.data.reason,parsed.data.idempotencyKey),redirectTo:`/app/events/${eventId}`});}catch(error){return lifecycleErrorResponse("remove_event_guest",error);}}
